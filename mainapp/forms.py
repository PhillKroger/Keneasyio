from django import forms
"""
from .models import Product

class PostForm(forms.ModelForm):
    title = forms.CharField(label='title', widget=forms.TextInput(attrs={'class': 'post_form_title', 'id': 'post_form_title_label'}))
    text = forms.CharField(label='text', widget=forms.TextInput(attrs={'class': 'post_form_text', 'id': 'post_form_text_label'}))

    class Meta:
        model = Product
        fields = ('title', 'text')
"""